---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Contacto Seguro · Corvus News">
  <style>
    .contact-container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 2rem;
    }
    
    .contact-hero {
      text-align: center;
      margin-bottom: 4rem;
      padding: 3rem 0;
      background: var(--bg-card);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border-primary);
    }
    
    .contact-title {
      font-family: var(--font-display);
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--text-primary);
    }
    
    .contact-subtitle {
      font-size: 1.25rem;
      color: var(--text-secondary);
      margin-bottom: 2rem;
    }
    
    .contact-methods {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin: 3rem 0;
    }
    
    .contact-method {
      background: var(--bg-card);
      border: 1px solid var(--border-primary);
      border-radius: var(--radius-lg);
      padding: 2.5rem;
      transition: var(--transition);
    }
    
    .contact-method:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-lg);
      border-color: var(--accent-primary);
    }
    
    .method-title {
      font-family: var(--font-display);
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--text-primary);
    }
    
    .method-description {
      color: var(--text-secondary);
      margin-bottom: 1.5rem;
      line-height: 1.6;
    }
    
    .method-details {
      background: var(--bg-secondary);
      border-radius: var(--radius-md);
      padding: 1.5rem;
      margin-bottom: 1.5rem;
    }
    
    .method-contact {
      font-family: 'Courier New', monospace;
      font-size: 1.125rem;
      color: var(--accent-primary);
      font-weight: 600;
      margin-bottom: 0.5rem;
      word-break: break-all;
    }
    
    .method-note {
      font-size: 0.875rem;
      color: var(--text-muted);
      font-style: italic;
    }
    
    .method-action {
      display: inline-flex;
      align-items: center;
      gap: 0.75rem;
      padding: 1rem 2rem;
      border-radius: var(--radius-xl);
      text-decoration: none;
      font-weight: 600;
      transition: var(--transition);
      font-size: 1.125rem;
      background: var(--accent-primary);
      color: var(--bg-primary);
    }
    
    .method-action:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }
    
    @media (max-width: 768px) {
      .contact-container {
        padding: 1rem;
      }
      
      .contact-title {
        font-size: 2rem;
      }
      
      .contact-methods {
        grid-template-columns: 1fr;
      }
    }
  </style>

  <br/><br/><br/>
  
  <div class="contact-container">
    <!-- Hero Section -->
    <section class="contact-hero">
      <h1 class="contact-title">Contacto Seguro</h1>
      <p class="contact-subtitle">
        Puedes contactarnos para colaboraciones, compartir noticias, filtrar información relevante o denunciar casos para su investigación.<br>
        Protege tu anonimato utilizando nuestros canales cifrados y consulta la <a href="/guia/" style="color:var(--accent-primary);text-decoration:underline;">Guía Segura</a> para enviar información de forma segura.
      </p>
    </section>

    <!-- Contact Methods -->
    <section class="contact-methods">
      <!-- ProtonMail Method -->
      <div class="contact-method">
        <h2 class="method-title">ProtonMail Seguro</h2>
        <p class="method-description">
          Puedes escribirnos directamente para colaborar, compartir una noticia o filtrar información de manera segura.<br>
          El correo está cifrado automáticamente y no requiere configuración técnica adicional.<br>
          Ideal para denuncias, evidencias y comunicación confidencial.
        </p>
        
        <div class="method-details">
          <div class="method-contact">corvusleaks@protonmail.com</div>
          <div class="method-note">Cifrado automático entre cuentas ProtonMail</div>
        </div>
        
        <a href="mailto:corvusleaks@protonmail.com" class="method-action">
          Enviar ProtonMail
        </a>
      </div>
    </section>
  </div>
</Layout>